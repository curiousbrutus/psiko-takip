/**
 * Local Database Configuration (replaces Firebase)
 *
 * This module previously contained Firebase configuration.
 * The application now uses local Oracle DB with JWT authentication.
 * All data operations go through /api/* routes.
 *
 * This file is kept as a stub to prevent import errors during migration.
 */

// No-op stubs - these should not be used anymore
// All pages should use the api-client instead
export const auth = null;
export const db = null;
export const storage = null;
export const app = null;
